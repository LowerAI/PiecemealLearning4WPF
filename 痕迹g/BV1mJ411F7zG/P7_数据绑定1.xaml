<Window x:Class="BV1mJ411F7zG.P7_数据绑定1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BV1mJ411F7zG"
        mc:Ignorable="d"
        Title="P7_数据绑定1" Height="450" Width="500">
    <Grid>
        <!--<TextBox x:Name="txt" FontSize="60" Text="{Binding Name,FallbackValue='找不到值'}" />-->

        <!--<StackPanel>
            <TextBox Text="{Binding Name}" FontSize="60" />
            <TextBox Text="{Binding Name}" FontSize="60" />
            <TextBox Text="{Binding Name}" FontSize="60" />
            <TextBox Text="{Binding Name}" FontSize="60" />
        </StackPanel>-->

        <StackPanel>
            <TextBox Text="{Binding Name}" FontSize="60" />
            <Button Command="{Binding SaveCommand}" Height="40" />
        </StackPanel>
    </Grid>
</Window>
